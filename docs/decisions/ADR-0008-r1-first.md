# ADR-0008 · V1 只交付 R1；R2 光斑标"潜力投影"

- 状态：Accepted
- 日期：2026-09-10

## 背景
窗多边形沿太阳向量投影是正确几何，但只给出潜力范围；精确裁剪需要外部遮挡的距离（视差），V1 没有。R1 已经是完整任务（这个点几点到几点）。

## 决定
V1 交付 R1；R2 在 V2 以"潜力投影"标签进入，虚线半透明，时段仍来自 R1。

## 备选
- V1 就做光斑：视觉比证据更确定，违反产品契约。

## 后果
V1 的 wow 来自照片 + 滑杆 + 时段条；spike 不做 R2，只在 RoomPlan 可用时记录对照数据。
